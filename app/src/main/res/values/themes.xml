<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema base sin dependencia de AppCompat (la UI real es Compose). -->
    <style name="Theme.UtilEs" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
