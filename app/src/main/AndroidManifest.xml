<?xml version="1.0" encoding="utf-8"?>
<!--
  Manifest ENDURECIDO (comparar con decompiled/resources/AndroidManifest.xml):

  - Sin usesCleartextTraffic (prohibido por network_security_config).
  - allowBackup=false + dataExtractionRules restrictivas (nada sale del dispositivo).
  - 3 permisos (frente a 31 del original); PROCESS_OUTGOING_CALLS y permisos
    muertos eliminados. CALL_PHONE no es necesario para sendUssdRequest (API 26+)
    pero se conserva documentado para el fallback del marcador en < Android 8.
  - Solo MainActivity exportado (launcher). Ningún receiver, service ni provider
    de la app; no hay point-of-entry para otras apps.
  - Sin profileable, sin SYSTEM_ALERT_WINDOW, sin USE_FULL_SCREEN_INTENT.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <!--
      CALL_PHONE: queda SOLO como reserva para marcado explícito de USSD en
      dispositivos < Android 8 (sendUssdRequest). Revisar cada release si sigue
      siendo necesario; no se usa para llamadas ordinarias.
    -->

    <application
        android:name=".UtilEsApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_utiles_logo"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.UtilEs"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.UtilEs">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Sin services, receivers, providers ni activity-alias exportados. -->
    </application>

</manifest>
